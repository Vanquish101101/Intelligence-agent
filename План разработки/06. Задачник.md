# 06. Задачник — Агент онлайн-разведчик

---

> ⚠️ **ВАЖНАЯ ПОМЕТКА О РАЗВИТИИ ДОКУМЕНТА**
>
> Задачник — живой документ. Новые задачи вписываются **в соответствующий раздел и этап** по смыслу, не в конец. Выполненные задачи отмечаются `[x]`, отложенные — `[~]`, заблокированные — `[!]`. Места для новых задач обозначены `[ + ]`.

---

## Статусы задач

| Статус | Обозначение |
|---|---|
| Не начата | `[ ]` |
| В работе | `[→]` |
| Выполнена | `[x]` |
| Отложена / v2 | `[~]` |
| Заблокирована | `[!]` |
| Недоступно сейчас | `[⛔]` |

---

## ЭТАП 0 — Подготовка и планирование

### Документация проекта
- [x] Написать файл 01. Идея.md
- [x] Написать файл 02. Вайр-фрейминг. Графическая схема.md
- [x] Написать файл 03. Брейншторм.md
- [x] Написать файл 04. ТЗ.md
- [x] Написать файл 05. Архитектура.md
- [x] Написать файл 06. Задачник.md
- [x] Написать файл 07. Запуск разработки.md
- [x] Написать файл 08. Тестирование.md
- [x] Написать файл 09. Деплой.md

`[ + ]` — *новые документы и артефакты*

### Открытые архитектурные решения (нужны до старта разработки)
- [x] Выбрать платформу развёртывания → **Hetzner VPS** (зарегистрирован)
- [x] Выбрать тип оркестратора → **гибрид: правила (algorithm.md) + LLM (OpenRouter)**
- [~] Выбрать систему очередей задач → откладывается на v1.5 (n8n или Redis BullMQ)
- [x] Выбрать формат реестра инструментов → **YAML файл** (`tool-registry/registry.yaml`)
- [x] Определить стратегию dev/prod → **local → prod напрямую** (без staging в MVP)
- [x] Выбрать хостинг-провайдера → **Hetzner** (зарегистрирован, не настроен)

`[ + ]` — *новые решения требующие принятия до старта*

---

## ЭТАП 1 — MVP (Первая рабочая версия)

### 1.1 Инфраструктура и окружение
- [⛔] Зарегистрировать и настроить VPS → Hetzner зарегистрирован, SSH-настройка требует ручного доступа
- [⛔] Установить Docker и Docker Compose → нужен физический доступ к серверу
- [⛔] Настроить Portainer → зависит от Docker
- [⛔] Создать docker-compose.yml со всеми сервисами → зависит от Docker
- [x] Настроить `.env` файл с API-ключами → готов в `Code/.env`
- [⛔] Настроить внутреннюю Docker-сеть → зависит от Docker
- [⛔] Проверить что все контейнеры стартуют → зависит от Docker

`[ + ]` — *новые инфраструктурные задачи*

### 1.2 Telegram Bot
- [x] Зарегистрировать бота через @BotFather → `@vanquish_intelligence_agent_bot`
- [x] Настроить Telegram Bot API → токен в `.env`
- [x] Реализовать команду `/start` — приветствие, список команд, выбор режима (inline-кнопки)
- [x] Реализовать команду `/mode` — переключение режима Информация / Создать контент
- [x] Реализовать команду `/report` — запрос полного отчёта через оркестратор
- [x] Реализовать команду `/search [тема]` — быстрый поиск через Perplexity
- [x] Реализовать команду `/settings` — просмотр и изменение настроек
- [x] Реализовать команду `/help` — справка по всем командам
- [x] Реализовать команду `/scrape <url>` — парсинг страницы
- [x] Реализовать команду `/transcribe <url>` — транскрибация видео
- [x] Реализовать команду `/trends <тема>` — быстрые тренды
- [x] Реализовать команду `/set_topics` — изменить темы мониторинга
- [x] Реализовать команду `/set_platforms` — изменить платформы
- [x] Реализовать команду `/set_depth` — изменить глубину анализа
- [x] Настроить whitelist пользователей (только авторизованные)
- [x] Статус "работаю..." во время длинных операций
- [→] Протестировать все команды вручную

`[ + ]` — *новые команды бота*

### 1.3 Оркестратор
- [x] Настроить Open Router (ключ в `.env`, HTTP-запросы)
- [x] Подключить LLM через Open Router (`anthropic/claude-haiku-4-5`)
- [x] Создать файл правил маршрутизации `algorithm.md`
- [x] Реализовать логику чтения правил и выбора инструментов
- [x] Реализовать базовый fallback (инструменты запускаются параллельно, ошибки не ломают цепочку)
- [x] Реализовать логику сборки финального отчёта (LLM + шаблон)
- [x] Поддержка типов задач: `report`, `trends`, `search`
- [→] Протестировать оркестратор на реальных запросах

`[ + ]` — *новые задачи оркестратора*

### 1.4 Реестр инструментов
- [x] Создать базовый реестр инструментов (`tool-registry/registry.yaml`)
- [x] Зарегистрировать Apify в реестре
- [x] Зарегистрировать Firecrawl в реестре
- [x] Зарегистрировать Perplexity в реестре
- [x] Зарегистрировать Whisper в реестре
- [x] Указать приоритеты и fallback для каждого инструмента
- [ ] Реализовать автоматический health check каждые 5 минут

`[ + ]` — *новые инструменты в реестре*

### 1.4.1 MCP-подключения (критично для MVP)
> Приоритет: 🔴 Критично — без этого агент не работает

- [x] **Apify MCP**: подключён через `npx @apify/actors-mcp-server` (в Claude Code)
- [x] **Perplexity MCP**: подключён через `@perplexity-ai/mcp-server` (в Claude Code)
- [x] **Whisper MCP**: кастомный сервер `Code/src/mcp-servers/whisper/index.js`
- [x] **Firecrawl MCP**: подключён через `firecrawl-mcp` (в Claude Code)
- [x] Настроить переменные окружения в `.env`
- [→] Протестировать каждый инструмент через оркестратор (бот вызывает API напрямую)

> Приоритет: 🟡 Важно — нужно до релиза MVP

- [ ] **LangSmith**: настроить трейсинг (`LANGCHAIN_TRACING_V2=true` + ключ)
- [ ] **Helicone**: проксировать OpenRouter запросы через Helicone endpoint

`[ + ]` — *новые MCP-подключения*

### 1.5 Агент разведчик (Агент 1) — ТОЛЬКО ПОИСК

> **Обновлено 2026-07-05:** Агент 1 = только поиск и нахождение источников. Парсинг → Агент 2. Задачи парсинга ниже переведены в `[~]` и перемещаются в проект `Deep parsing agent`.

**Остаётся в Агенте 1 (поиск):**
- [x] Реализован модуль `agents/scout/index.js`
- [x] Поиск через Perplexity API (факты, тренды, актуальность)
- [x] Параллельный запуск всех источников (`Promise.allSettled`)
- [x] Обработка ошибок без падения всей цепочки
- [ ] Получение готовых субтитров YouTube (youtube-transcript-api — без скачивания)
- [→] Тест на реальных поисковых запросах

**Переходит в Агент 2 (Deep parsing agent):**
- [~] YouTube тренды через Apify (youtube-scraper actor) → Агент 2
- [~] Парсинг сайтов через Firecrawl → Агент 2
- [~] Парсинг Telegram каналов (Apify actor) → Агент 2
- [~] Парсинг VK (Apify actor) → Агент 2
- [~] Парсинг TikTok (Apify actor) → Агент 2

**Новые задачи: переделка под модель «только поиск»:**
- [ ] Убрать из `agents/scout/index.js` прямые вызовы Firecrawl как парсера (оставить только поиск)
- [x] Добавить логику передачи задачи Агенту 2 через Redis (`queue:deep-parsing:jobs`) — модуль `agents/router/agent2Queue.js`, вызов из оркестратора fire-and-forget
- [x] Подписаться на `notifications:agent1` Redis-канал — Агент 2 уведомляет о сбоях передачи Агенту 3 (в `telegram-bot/index.js`, subscriber-соединение)
- [ ] Добавить `youtube-transcript-api` вместо Apify для быстрых субтитров
- [ ] Обновить `algorithm.md` — новые правила маршрутизации (что сам, что → Агент 2)
- [ ] Протестировать: поиск работает, сложный контент корректно передаётся в очередь Агента 2

`[ + ]` — *новые задачи поиска*

### 1.6 Агент обработчик → ПЕРЕЕЗЖАЕТ в Deep parsing agent

> ⚠️ **Статус (обновлено 2026-07-05):** Модуль `agents/transcriber/index.js` (транскрибация + анализ) реализован в Агенте 1 как временное решение «всё в одном». По новой архитектуре эта функциональность **полностью переходит** в отдельный проект `Deep parsing agent` (Агент 2). Код может быть использован как основа.

**Что уже реализовано (перенести в Агент 2):**
- [~] Модуль `agents/transcriber/index.js` → переезжает в `Deep parsing agent/Code`
- [~] Транскрибация через OpenAI Whisper API → Агент 2
- [~] Анализ транскрипта через LLM → Агент 2
- [~] Скачивание файла с URL → Агент 2

**Что остаётся в Агенте 1 (взаимодействие с Агентом 2):**
- [x] Передавать задачи Агенту 2 с контекстом (URL + тип + query) — через `routeToAgent2()` в оркестраторе
- [x] Подписаться на уведомления Агента 2 через Redis pub/sub (`notifications:agent1`)
- [ ] Принимать результаты Агента 2 и вставлять в отчёт пользователю
- [ ] Отображать статус "Агент 2 обрабатывает..." в Telegram

`[ + ]` — *задачи по миграции в Агент 2*

### 1.7 Трёхуровневая обработка данных
- [x] Уровень 1: сбор сырых данных (Apify + Firecrawl + Perplexity)
- [x] Уровень 2: базовая фильтрация и агрегация (scout agent)
- [x] Уровень 3: анализ смыслов и формирование отчёта (LLM через OpenRouter)
- [x] Связать три уровня в единую цепочку (orchestrate → scout → LLM)
- [~] Кэширование результатов (6 часов) → v1.5 Redis
- [→] Тест полного цикла от запроса до отчёта

`[ + ]` — *новые задачи по обработке данных*

### 1.8 Формирование и отправка отчёта
- [x] Реализовать шаблон отчёта (формат из ТЗ: тренды, платформы, идеи, проверить)
- [x] Отправка отчёта в Telegram с учётом лимита 4096 символов
- [x] Статус "работаю..." во время обработки (editMessage)
- [x] Метаданные в конце отчёта (время, инструменты, ошибки)
- [x] Частичный отчёт при сбое части источников (ошибки не ломают отчёт)
- [x] Пометка о недоступных источниках в отчёте

`[ + ]` — *новые элементы отчёта*

### 1.9 Автозапуск по расписанию
- [ ] Настроить n8n workflow с trigger по расписанию
- [ ] Реализовать ежедневный вызов `/report` по расписанию
- [ ] Реализовать настройку времени через `/settings`
- [ ] Протестировать автозапуск в реальных условиях

`[ + ]` — *новые задачи по расписанию*

### 1.10 Логирование и мониторинг
- [x] Базовое логирование шагов (console.log в оркестраторе и агентах)
- [x] Логирование ошибок и fallback-переключений (поле `errors` в meta)
- [x] Уведомление пользователя при полном сбое (catch + editMessage)
- [ ] Логирование расходов API по каждому вызову
- [ ] Полноценная система логов (файлы / LangSmith)

`[ + ]` — *новые задачи мониторинга*

### 1.12 Выбор режима работы + 5-шаговый Wizard (v0.2.5)

> Добавлено 2026-07-09. Реализует выбор между тремя режимами при старте + диалоговый wizard.
> Коммит `cae087f` — «Wizard: 3 режима + 5-шаговый wizard + модерация».

**Клавиатура режимов:**
- [x] `MODE_KEYBOARD` — inline-клавиатура с **тремя** кнопками (2 ряда) при `/start` и `/mode`
- [x] Добавить кнопку `/mode` в `MAIN_KEYBOARD` (reply keyboard)
- [x] Команда `/mode` — показывает текущий режим + inline-клавиатуру для переключения
- [x] `bot.action('mode_info')` — сохраняет `mode: 'info'` в `user_settings`, подтверждает режим; сбрасывает `wizardState`
- [x] `bot.action('mode_content')` — запускает `startWizard(ctx, 'content')` (был заглушкой — теперь wizard)
- [x] `bot.action('mode_publish')` — **новый третий режим** «🚀 Создать и опубликовать», запускает `startWizard(ctx, 'publish')`
- [x] `/mode` добавлена в `setMyCommands()` (меню Telegram)

**7-шаговый Wizard (`wizardState = new Map()`), обновлено 2026-07-12/13:**
- [x] `startWizard(ctx, mode)` — устанавливает `{step:1, mode, use_trends:null, project:null, networks:[]}` в `wizardState`, отправляет шаг 1 с `WIZARD_TRENDS_KB`
- [x] **Шаг 1 (2026-07-10) — опора на тренды** (`bot.action(/^wiz_trends_(yes|no)$/)`): «🔥 На основе трендов» / «📝 Просто по описанию» → `wiz.use_trends`. См. «Тренды в wizard'е» ниже.
- [x] **Шаг 2 (новый, 2026-07-12) — выбор PostMyPost-проекта** (`bot.action(/^wiz_proj_(.+)$/)`, `WIZARD_PROJECT_KB`): «📢 Marketing» / «🏠 Project CORE» → `wiz.project`. См. «Мультивыбор соцсетей + PostMyPost-проект» ниже.
- [x] **Шаг 3 (переработан, 2026-07-12) — мультивыбор соцсетей** (`bot.action(/^wiz_nettoggle_(.+)$/)` + `bot.action('wiz_netdone')`, `buildNetworkKeyboard()`): Instagram/YouTube/TikTok/Telegram/ВК/WhatsApp/Одноклассники — toggle-кнопки с ✅/⬜, перерисовка того же сообщения (`editMessageReplyMarkup`) на каждый клик, отдельная кнопка «Готово» (заблокирована, пока не выбрана хотя бы одна сеть) → `wiz.networks` (массив)
- [x] Шаг 4 — тип контента (`bot.action(/^wiz_type_(.+)$/)`): пост/видео/фото/аудио/Reels/Карусель
- [x] Шаг 5 — формат/соотношение сторон (`bot.action(/^wiz_fmt_(.+)$/)`): 9:16/16:9/1:1/4:5/текст
- [x] Шаг 6 — стиль подачи (`bot.action(/^wiz_style_(.+)$/)`): экспертный/дружелюбный/провокационный/вдохновляющий
- [x] Шаг 7 — свободное описание задачи (перехват в `bot.on('text')` перед обычным поиском)
- [x] После шага 7: `updateSetting(userId, 'wizard', {project, networks, content_type, format, style, description, use_trends})` — сохранение в `user_settings`
- [x] Для `mode_publish`: показывает inline-кнопку переключения модерации (`toggle_moderation`) после итогового summary

**Мультивыбор соцсетей + PostMyPost-проект (2026-07-12/13, по прямому запросу пользователя):**
- **Что было.** `wizard.network` — одна строка, выбор из 7 кнопок, одна кнопка = сразу следующий шаг.
  `POSTMYPOST_PROJECT_ID` у Агента 4 — один захардкоженный env var. Физически нельзя было
  опубликовать в несколько сетей за раз или выбрать, в какой из двух PostMyPost-проектов
  пользователя (найдено в этой же сессии — «Marketing» 351825 и «Project CORE» 347765, у каждого
  свои подключённые соцсети) публиковать.
- [x] `wiz.network` → `wiz.networks` (массив) + новый `wiz.project` — оба поля идут в
  `wizardSettings`, `content_ready`/`moderation_request` отображение (`formatAgent4Message`)
  переведено на них.
- [x] `wizardHash()` — `WIZARD_HASH_FIELDS` теперь `['project', 'networks', ...]` вместо
  `['network', ...]`; добавлена `serializeField()` — массивы сериализуются через сортировку (не
  `.toString()`), чтобы порядок кликов пользователя по сетям не влиял на хеш. **Проверено живым
  кросс-вызовом** этой функции и зеркальной `Content creation agent/Code/src/wizard/hash.js::
  computeWizardHash()` на 3 разных wizard'ах — все хеши совпали побайтово (не повторили историю
  прежнего расхождения формата, см. запись выше от 2026-07-10).
- [x] Синтаксис (`node --check`) проверен на обоих изменённых файлах — тестовой инфраструктуры
  в этом репозитории нет (как и раньше), полноценного клика через реальный Telegram-интерфейс
  ещё не было (у меня нет доступа к Telegram) — контейнер передеплоен (`v0.3.0`), стартует и
  подписывается на Redis без ошибок в логах.
- Подробности стороны Агента 4 (публикация по нескольким сетям, резолвинг `project_id`,
  339/339 тестов) — в его собственном Задачнике, «08. Задачник.md».

**Меню тем мониторинга + новые темы по умолчанию (2026-07-13, по прямому запросу пользователя):**
- **Что было.** Темы менялись только текстовой командой `/set_topics тема1, тема2` — рабочий, но не
  визуальный способ. Пользователь также попросил отменить "регулярный анализ в 6:00 утра" — **не
  найден нигде** ни в коде (`grep` по `cron`/`schedule`/`06:00` — 0 совпадений), ни в файлах
  планирования; папка `src/scheduler/` существует, но пуста (только `.gitkeep`, никогда не
  реализовывалась). Единственный реально работающий агент — `telegram-bot/index.js`
  (`npm start` → `node src/telegram-bot/index.js`), там нет ежедневных задач, кроме опроса очереди
  Агента 4 раз в 5 минут. **Открытый вопрос пользователю**: где именно крутится эта задача (если
  вообще существует) — вне этого кода (n8n/сервер) или это было только в планах?
- [x] `DEFAULT_SETTINGS.topics` (`src/db/userSettings.js`) — изменён с `['маркетинг', 'крипта',
      'партнёрки']` на `['маркетинг', 'инвестиции в РФ', 'тренды и контент соцсетей']`.
- [x] Новая команда `/topics` + `TOPIC_CATALOG` (6 тем) + `buildTopicsKeyboard()` — тот же паттерн
      toggle-кнопок с ✅/⬜/перерисовкой сообщения, что и мультивыбор соцсетей у Content creation
      agent (см. выше) — `bot.action(/^topic_toggle_(\d+)$/)` переключает тему в `user_settings`,
      `bot.action('topic_done')` подтверждает. Команда добавлена в `MAIN_KEYBOARD` и
      `setMyCommands()`. `/set_topics` не заменён — свободная тема не из каталога всё ещё
      добавляется текстом.
- **Осознанно не сделано** (по прямому решению пользователя после уточняющего вопроса, см. чат):
  реальные поисковые интеграции для ВК/Одноклассники/Телеграм/ТикТок/Инстаграм — `scout()`
  (`src/agents/scout/index.js`) сейчас распознаёт только `platforms.includes('youtube')` и
  `.includes('web')`, остальные значения были бы молчаливой пустышкой. Пользователь выбрал начать
  только с меню тем, соцсети — отдельная, гораздо большая задача на будущее (нужны отдельные
  интеграции под каждую сеть — Apify-акторы для TikTok/Instagram, Telethon/pyrogram для Telegram,
  официальный VK API, для Одноклассников готового варианта не найдено даже в раннем исследовании).

**Меню тем для /trends (2026-07-13, по прямому запросу пользователя):**
- **Что было.** `/trends` без аргумента только просил ввести тему текстом (`/trends крипта`) —
  выбора не было, только свободный ввод.
- [x] `buildTrendsTopicKeyboard()` — та же `TOPIC_CATALOG`, что и у `/topics`, но клик сразу
  запускает анализ по выбранной теме (разовое действие, `bot.action(/^trends_topic_(\d+)$/)`), а
  не переключает настройку `user_settings.topics`, как `topic_toggle_*`. Логика самого анализа
  вынесена в `runTrendsAnalysis(ctx, topic)` — общая для текстового ввода и клика по кнопке.
  Свободный ввод (`/trends своя тема`) по-прежнему работает как раньше, меню — не замена, а
  быстрый путь для частых тем.

**Тренды в wizard'е (2026-07-10, по прямому указанию пользователя — замена эвристики Агента 4):**
- **Что было.** Агент 4 угадывал, нужно ли обогащение трендами, по ключевым словам в свободном
  описании (`тренд`/`виральн`/`хайп`/...) — ненадёжно, непрозрачно, пользователь не мог явно
  выбрать. Живой тест 2026-07-10 показал: даже когда угадывание срабатывало, само обогащение почти
  всегда возвращало `null` (см. Content creation agent, «Доработки для агентов», пункт 13).
- [x] Явный вопрос на шаге 1 (см. выше) вместо угадывания — `wizard.use_trends` теперь единственный
  источник истины.
- [x] Когда `use_trends === true` — по завершении wizard'а (шаг 6) fire-and-forget запускается
  `orchestrate({ task_id, user_id, type: 'trends', topics: [описание], platforms: ['youtube','web'],
  depth: 'quick' })` — тот же путь, что и у `/trends`: реальный поиск → `search_results` → маршрутизация
  тяжёлых находок Агенту 2 (`routeToAgent2` внутри `orchestrate`). Агент 4 подхватывает результат
  через дайджест Агента 3 с ретраями на своей стороне.
- [x] Когда `use_trends === false` — ничего из вышеперечисленного не запускается вообще (ни одного
  обращения к Агентам 2/3) — контент генерируется сразу по описанию, без ошибок с любой стороны.
- [x] Итоговый summary после wizard'а показывает выбранный подход (`🔥 Подход: ...`) и предупреждает
  о более долгом ожидании (`может занять пару минут`), когда выбраны тренды.
- [x] `wizardHash()` (`src/handoff/agent4Handoff.js`) — переписан на точный формат Агента 4
  (`key=value` через `\n`, было `a|b|c`) + добавлено поле `use_trends`. **Побочная находка**: два
  независимых написания хеша никогда не совпадали ни при одном реальном запросе — не ломало
  функциональность (у Агента 4 есть fallback на "actual"), но засоряло логи ложным
  предупреждением "wizard_hash mismatch" на каждый запрос без исключения.
- [x] `notifications:agent1` (Агент2→Агент1) — учитывает новое поле `payload.buffered` от Агента 2
  (см. его Задачник, пункт про `ensureJobRow`) — текст больше не утверждает безусловно "данные не
  потеряны", когда резервная запись сама могла не удаться.

**Управление модерацией:**
- [x] `bot.action('toggle_moderation')` — читает текущее `moderation_mode` из `user_settings`, инвертирует, обновляет inline-клавиатуру через `ctx.editMessageReplyMarkup`

**Тестирование:**
- [ ] Проверить что при `/start` приходят приветствие + кнопки трёх режимов
- [ ] Нажать «🔍 Получить информацию» — бот подтверждает режим, wizard сбрасывается
- [ ] Нажать «🎬 Создать контент» — запускается wizard шаг 1 (тренды)
- [ ] Нажать «🚀 Создать и опубликовать» — запускается wizard шаг 1 + после шага 6 показывается кнопка модерации
- [ ] Шаг 2 — выбрать проект (Marketing/Project CORE)
- [ ] Шаг 3 — потыкать несколько сетей подряд, убедиться что ✅ появляются/пропадают на СВОЁМ сообщении (не плодит новые), «Готово» без выбора не пускает дальше
- [ ] Пройти все 7 шагов wizard'а до конца, убедиться что `user_settings.wizard` сохраняется в Supabase с полями `project`/`networks` (массив)
- [ ] Переключить модерацию — кнопка обновляется inline без нового сообщения

**Обратный канал Агент 4 → Агент 1 (v0.2.6):**
- [x] Подписка на Redis `notifications:agent1_from_agent4` — быстрый слой (mirror паттерна `notifications:agent1` от Агента 2)
- [x] `pollAgent4DeliveryQueue()` — polling `content_creation_agent.agent1_delivery_queue` каждые 5 мин, catch-up для пропущенных Redis-событий (кросс-схемное чтение через `.schema('content_creation_agent')`)
- [x] `formatAgent4Message(messageType, payload)` — форматирует 3 типа сообщений: `content_ready` / `quota_warning` / `moderation_request`
- [x] Deduplication через `_deliveredAgent4Ids` Set (cap 500) + `status = 'delivered'` в Supabase после polling-доставки
- [x] Первый прогон polling при старте бота (catch-up накопленного за время downtime)
- [x] Фикс полей `content_ready`: camelCase (`contentType`/`sizeBytes`), `downloadUrl` вместо массива `files[]`, показ `text`/`costUsd`/`publishReport`
- [x] Фикс полей `quota_warning`: `userUsageBytes`/`totalUsageBytes`/`limitBytes`/`items[]` вместо snake_case
- [x] Фикс `moderation_request`: `downloadUrl` (presigned, рабочая) как основная ссылка, `r2Url` — fallback для логов
- [x] Фикс Redis fast-path: `envelope.payload ?? {}` передаётся в `formatAgent4Message`, а не весь envelope (раньше поля никогда не совпадали)

**Канал согласия пользователя → Агент 4 (пункт G, v0.2.6):**
- [x] Миграция `004_agent4_consent.sql` — таблица `intelligence_agent.agent4_consent_queue` (id/telegram_id/generated_content_id/decision_type/decision/attempt_count/last_attempt_at/status/created_at), применена к живой БД
- [x] `getAgent4Keyboard(messageType, payload)` — возвращает `inline_keyboard` для `quota_warning` («✅ Удалить старое»/«❌ Отмена») и `moderation_request` («✅ Опубликовать»/«❌ Отклонить»); callback_data: `cqa_qd_<uuid>` / `cqr_qd_<uuid>` / `cqa_pm_<uuid>` / `cqr_pm_<uuid>` (макс. 43 байта, в пределах лимита Telegram 64)
- [x] `sendAgent4MessageToUser` — теперь включает `reply_markup` с keyboard когда доступно; fallback на plain text сохраняет keyboard
- [x] `bot.action(/^(cqa|cqr)_(qd|pm)_([0-9a-f-]{36})$/)` — handler кнопок: `answerCbQuery` + убирает кнопки у исходного сообщения + INSERT в `agent4_consent_queue` + publish в Redis `notifications:agent4_from_agent1` с `{ event:"decision_ready", ... }` под строгий формат Агента 4
- [x] Агент 4 добавил `generatedContentId` в payload `moderation_request` (раньше было только в Supabase-колонке, теперь и на Redis-пути)
- [x] Тексты `quota_warning`/`moderation_request` — убраны фразы «в разработке», заменены на инструкцию нажать кнопку
- [x] Агент 4 (`consent/subscribe.js`, `consent/poller.js`, `consent/handleDecision.js`) — уже полностью готов и ждёт решений от Агента 1

**Оставшееся:**
- [ ] В `/report` учитывать текущий `mode` и добавлять шаг создания контента (Агент 4)

`[ + ]` — *новые задачи*

---

### 1.11 Структурированный вывод для Агента 3

> Добавлено 2026-07-07. Задачи вытекают из файла `Доработки для Агента 1 (передать).md`.

**БД:**
- [x] Создать таблицу `intelligence_agent.search_results` (migration 002)
- [x] Применить миграцию в Supabase (`Code/src/db/migrations/002_search_results.sql`)

**Оркестратор (`Code/src/orchestrator/index.js`):**
- [x] Добавить `buildStructuredResult(scoutResult)` — собирает `result.raw` из сырых данных Scout
- [x] Добавить `buildConfidence(scoutResult)` — уровень уверенности по числу ошибок
- [x] Добавить `saveSearchResult(payload)` — fire-and-forget сохранение в `search_results`
- [x] Возвращать `job_id`, `source_type: 'search'`, `result.raw`, `confidence` вместо только `report`
- [x] `report` (Telegram-текст) сохранён как поле `telegram_text` в БД (backwards compat)

**Тестирование:**
- [ ] Запустить `/report` в боте, проверить запись в Supabase `search_results`
- [ ] Проверить что `result.raw.perplexity`, `result.raw.youtube`, `result.raw.firecrawl` заполнены
- [ ] Проверить что Telegram-бот по-прежнему работает (использует `result.report`)
- [ ] Проверить что при сбое одного источника `confidence.level` = 'средняя', не ломает сохранение

**Выполнено на стороне Агента 3 (2026-07-09):**
- [x] Агент 3 читает из `intelligence_agent.search_results` через `.schema('intelligence_agent').from(...)` — исправлен баг cross-schema подключения (коммиты `304577c`/`b5126f5`), живая проверка подтверждена (1 реальный элемент прочитан из прода)
- [x] Агент 3 использует `result.raw` для структурированного анализа, `telegram_text` как fallback — реализовано в `agent1Reader.js`

`[ + ]` — *новые задачи структурированного вывода*

---

## ЭТАП 2 — Стабилизация (v1.5)

- [ ] Добавить очередь задач (Redis + BullMQ или n8n)
- [ ] Настроить кэширование результатов парсинга (6 часов)
- [ ] Реализовать дашборд расходов API
- [ ] Настроить автоматический health check всех инструментов
- [ ] Подключить Supabase для хранения настроек и истории отчётов
- [ ] Реализовать историю отчётов (команда `/history`)
- [ ] Оптимизировать выбор моделей по стоимости через Open Router
- [ ] Провести нагрузочное тестирование
- [ ] Настроить LangSmith трейсинг
- [ ] Подключить Helicone для мониторинга расходов

`[ + ]` — *новые задачи этапа стабилизации*

---

## ЭТАП 3 — Расширение (v2.0)

- [ ] Реализовать MCP-интерфейс для подключения к Marketing agency
- [ ] Добавить парсинг TikTok, VK, Telegram через Apify
- [ ] Реализовать алерты на хайп в реальном времени
- [ ] Реализовать мониторинг конкурентов
- [ ] Добавить мультиязычный поиск (EN + RU)
- [ ] Реализовать голосовые отчёты (ElevenLabs)
- [ ] Добавить платформы: Одноклассники, Twitter/X, Facebook
- [ ] Реализовать сравнение периодов (эта неделя vs прошлая)
- [ ] Автоматическое обнаружение новых инструментов через Smithery

`[ + ]` — *новые задачи v2.0*

---

## ЭТАП 4 — Масштабирование (v3.0)

- [ ] Перенос на Kubernetes / облачный auto-scaling
- [ ] Реализовать мультипользовательность
- [ ] White-label упаковка для продажи
- [ ] Биллинг и управление подписками
- [ ] Горизонтальное масштабирование агентов

`[ + ]` — *новые задачи масштабирования*

---

## Интеграции (отдельным треком)

- [ ] Подключить Smithery для поиска новых MCP-инструментов
- [ ] Интеграция с главным оркестратором Marketing agency
- [ ] Интеграция с Контент-заводом (передача трендов)
- [ ] Интеграция с Notion API (выгрузка отчётов)
- [ ] Настроить резервное копирование базы данных

`[ + ]` — *новые интеграции*

---

## Открытые вопросы (блокируют задачи)

- [x] **Платформа развёртывания** → Hetzner VPS (зарегистрирован)
- [→] **Настройка VPS** → требует SSH-доступа и ручной работы с сервером
- [!] **Docker** → не установлен, блокирует контейнеризацию
- [~] **Система очередей** → откладывается на v1.5
- [ ] **Механизм интеграции с Marketing agency** (MCP-экспорт агента)
- [ ] **Переделка Агента 1 под модель «только поиск»** (добавлено 2026-07-05, решено во время брейншторма Агента 2 в проекте `Deep parsing agent`): Firecrawl/Apify/Whisper-транскрибация (раздел 1.6 выше) должны переехать в отдельный проект `Deep parsing agent` — Агент 1 должен остаться только с поиском (Perplexity + выбор источников), парсингом вообще не заниматься. Подробности решения — в `Deep parsing agent/План разработки/5. Брейншторм (Глубокий разбор).md`, раздел 2, и в файле памяти Claude `four_agent_architecture_split.md`. Также решено: Redis теперь общий на все агенты (Docker, сеть `marketing-agency-net`, файл `Marketing agency Project/Инфраструктура (Docker)/docker-compose.yml`) и Supabase теперь общий проект `Marketing agency` со схемой `intelligence_agent` — использовать их вместо локальных заготовок из этого файла (строки про v1.5 Redis/Supabase выше).

`[ + ]` — *новые блокирующие вопросы*
